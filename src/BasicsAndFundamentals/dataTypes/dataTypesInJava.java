package BasicsAndFundamentals.dataTypes;

public class dataTypesInJava {

        /*
         Data Types
         1. Primitive Data Types - Primitive Data types are the ones which store actual values and these data types
            do not store null values. They store the value directly in stack.
            a. byte - To be used when working with large arrays where memory saving is critical, it can store values
               between -128 -> 127 (i.e age of a person)
            b. short - use short for small integer data types ranging from -32,768 -> 32,767 (temperature values)
            c. int - use to store whole numbers which are larger than byte and short, it can store values between
               -2,147,483,648 -> 2,147,483,647 (-2^31 to 2^31 - 1)
            d. long - used to store whole numbers if the numbers are great then range of byte, short and int, and it
               ranges between -9,223,372,036,854,775,808 to 9,223,372,036,854,775,807 (-2^63 to 2^63 - 1)
            e. float - float is used to store decimal values with small precision up-to 7 decimal places.
            f. double - float is used to store decimal values with larger precision up-to 15 decimal places.
            g. char - use to store character, it's mainly used to take instruction from user through keyboard
            h. boolean - used to hold a single value true of false, it's used in conditional and branching logic

         2. Reference Data Types - Reference or non-Primitive data types are the ones which do not store the actual values
            instead they store reference to actual values. They can store. They store actual data in heap memory but
            store the reference to that data on stack.
            a. string - a reference data type that store and handles text data
            b. array - arrays are used to store list of same type of items, and it uses a single variable to represent them
            c. enum - a special data type used to define a list of named values and values are treated as options.
            d. class - a class is a blueprint for creating objects, it contains properties (data, variables) and
               behaviour (methods, actions performed), it groups together same type of actions.
            e. interface - interface provides function declaration with no definition. All methods in an interface are
               empty by default
            f. object - objects are the implementation or also called instances of classes which include real world data
        */

    public static void main(String[] args) {

    }
}